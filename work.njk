---
layout: layouts/base.njk
title: Work — Good and Bad
---

{% block head %}
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant:wght@400&family=Inter:wght@400&family=IBM+Plex+Mono:wght@400&display=swap" rel="stylesheet">
<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        background: #0a0a0a;
        color: #ffffff;
        font-family: 'Inter', sans-serif;
        line-height: 1.6;
    }

    .container {
        max-width: 1600px;
        margin: 0 auto;
        padding: 40px 20px;
    }

    .header {
        text-align: center;
        margin-bottom: 60px;
        padding-bottom: 40px;
        border-bottom: 1px solid #333;
    }

    .header h1 {
        font-size: 2.5em;
        font-weight: 300;
        margin-bottom: 20px;
        letter-spacing: 2px;
    }

    .header p {
        font-size: 1em;
        color: #999;
        max-width: 800px;
        margin: 0 auto;
        line-height: 1.7;
    }

    .section-title {
        font-size: 1.8em;
        font-weight: 300;
        margin: 80px 0 40px 0;
        padding-bottom: 20px;
        border-bottom: 1px solid #222;
        letter-spacing: 1px;
    }

    .mockup-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(480px, 1fr));
        gap: 60px;
        margin-bottom: 60px;
    }

    .mockup {
        background: #111;
        border: 1px solid #222;
        padding: 40px;
        border-radius: 8px;
        min-height: 500px;
    }

    .mockup-title {
        font-size: 1.1em;
        margin-bottom: 8px;
        color: #fff;
        font-weight: 500;
        letter-spacing: 1px;
    }

    .mockup-subtitle {
        font-size: 0.85em;
        color: #666;
        margin-bottom: 35px;
    }

    /* CONSTANTS APPLIED TO ALL */
    .work-list {
        list-style: none;
    }

    .work-item {
        font-size: 0.95rem;
        line-height: 1.7;
        transition: opacity 0.3s ease;
    }

    .work-item:hover {
        opacity: 1 !important;
    }
    
    /* Dim all items when a linked item is hovered */
    .work-list:has(a:hover) .work-item {
        opacity: 0.4;
    }
    
    /* Keep the hovered item at full opacity */
    .work-list:has(a:hover) .work-item:hover {
        opacity: 1;
    }

    /* Unlinked items don't get hover effects on the text itself */
    .work-item:not(:has(a)) .project-name {
        cursor: default;
    }

    .project-name a {
        color: #fff;
        text-decoration: none;
        font-weight: 400;
        transition: all 0.3s ease;
    }

    .separator {
        color: #666;
    }

    .meta {
        color: #666;
        font-weight: 400;
    }

    /* VARIABLE 1: FONT FAMILY */
    .font-inter { font-family: 'Inter', sans-serif; }
    .font-cormorant { font-family: 'Cormorant', serif; }
    .font-mono { font-family: 'IBM Plex Mono', monospace; }

    /* VARIABLE 2: VERTICAL SPACING */
    .spacing-tight .work-item { margin-bottom: 14px; }
    .spacing-medium .work-item { margin-bottom: 20px; }
    .spacing-loose .work-item { margin-bottom: 28px; }

    /* VARIABLE 3: HOVER EFFECT */
    .hover-underline .project-name a {
        border-bottom: 1px solid #333;
    }
    .hover-underline .project-name a:hover {
        border-bottom-color: #fff;
    }

    .hover-color .project-name a:hover {
        color: #ccc;
    }

    .hover-brackets .project-name a::before {
        margin-right: 2px;
    }
    .hover-brackets .project-name a::after {
        margin-left: 2px;
    }
    .hover-brackets .project-name a:hover::before {
        content: '[';
    }
    .hover-brackets .project-name a:hover::after {
        content: ']';
    }

    /* VARIABLE 4: SEPARATOR TYPE */
    .sep-comma .separator::after { content: ','; }
    .sep-comma .separator { margin-left: 0; margin-right: 6px; }
    
    .sep-slash .separator::after { content: '/'; }
    .sep-slash .separator { margin-left: 8px; margin-right: 8px; }
    
    .sep-dash .separator::after { content: '—'; }
    .sep-dash .separator { margin-left: 8px; margin-right: 8px; }
    
    .sep-tab .work-item { 
        display: grid;
        grid-template-columns: 1fr auto auto auto auto;
        gap: 24px;
        align-items: baseline;
    }
    .sep-tab .separator { display: none; }
    .sep-tab .meta-col {
        color: #666;
        font-weight: 400;
    }
    
    /* Make tab-aligned work-list a single grid for proper alignment */
    .sep-tab.work-list {
        display: grid;
        grid-template-columns: 1fr auto auto auto auto;
        gap: 24px;
        row-gap: 20px;
    }
    .sep-tab.work-list .work-item {
        display: contents;
    }

    /* VARIABLE 5: METADATA (handled in HTML) */
    /* year-only, year-collab, full */

    /* GRID FORMAT (separate from inline) */
    .format-grid .work-item {
        display: grid;
        grid-template-columns: 70px 1fr 100px;
        gap: 20px;
        padding: 14px 0;
        border-bottom: 1px solid #1a1a1a;
        align-items: baseline;
        transition: all 0.3s ease;
    }

    .format-grid .work-item:hover {
        background: #151515;
        padding-left: 12px;
        margin-left: -12px;
        padding-right: 12px;
        margin-right: -12px;
    }

    .format-grid .year {
        color: #666;
        font-variant-numeric: tabular-nums;
    }

    .format-grid .project-info {
        display: flex;
        flex-direction: column;
        gap: 3px;
    }

    .format-grid .project-name a {
        color: #fff;
        text-decoration: none;
        font-weight: 400;
    }

    .format-grid .project-name a:hover {
        color: #ccc;
    }

    .format-grid .collaborator {
        font-size: 0.9em;
        color: #666;
    }

    .format-grid .type {
        font-size: 0.85em;
        color: #777;
        text-align: right;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        font-weight: 300;
    }

    @media (max-width: 768px) {
        .mockup-grid {
            grid-template-columns: 1fr;
        }
    }
</style>
{% endblock %}

<div class="container">
    <div class="header">
        <h1>SYSTEMATIC EXPLORATION</h1>
        <p>Exploring 6 core variables while keeping all other parameters constant. Each section isolates one variable for direct comparison. All use the same constants: 0.95rem size, 400 weight, 1.7 line-height, 0.3s transitions, 0.4 opacity dimming.</p>
    </div>

    <div id="content"></div>
</div>

{% block scripts %}
<script>
    // DATA SOURCE - Edit this array to change all mockups
    const workItems = [
        { title: "Work Title", year: "Year", location: "(Location)", collaborator: "(Collaborator)", type: "(Type)", hasLink: true },
        { title: "Archive Grid", year: "2025", location: "Digital", collaborator: "", type: "Campaign", hasLink: true },
        { title: "REPLICA MAN", year: "2025", location: "Print", collaborator: "", type: "Cover", hasLink: true },
        { title: "Vogue Italia Spring Campaign", year: "2024", location: "", collaborator: "Mario Testino", type: "Editorial", hasLink: true },
        { title: "Paris Fashion Week SS25", year: "2024", location: "Paris", collaborator: "", type: "Runway", hasLink: false },
        { title: "Studio XYZ Collaboration", year: "2023", location: "London", collaborator: "", type: "Limited Collection", hasLink: true },
        { title: "W Magazine Cover Story", year: "2023", location: "New York", collaborator: "", type: "Editorial", hasLink: true }
    ];

    // Helper function to format metadata for inline formats
    function formatInlineMeta(item, metadataLevel) {
        const parts = [item.year];
        
        if (metadataLevel === 'full') {
            if (item.location) parts.push(item.location);
            if (item.collaborator) parts.push(item.collaborator);
            if (item.type) parts.push(item.type);
        } else if (metadataLevel === 'year-collab') {
            if (item.location) parts.push(item.location);
            if (item.collaborator) parts.push(item.collaborator);
        }
        // 'year-only' just uses the year
        
        return parts.filter(p => p).join(', ');
    }

    // Helper function to format collaborator for grid
    function formatGridCollaborator(item) {
        const parts = [];
        if (item.location) parts.push(item.location);
        if (item.collaborator) {
            parts.push(item.collaborator.startsWith('with ') ? item.collaborator : `with ${item.collaborator}`);
        }
        return parts.join(', ');
    }

    // Generate inline format HTML
    function generateInlineList(classes, metadataLevel = 'full') {
        return `<ul class="work-list ${classes}">
            ${workItems.map(item => `
                <li class="work-item">
                    <span class="project-name">${item.hasLink ? `<a href="${item.title === 'Archive Grid' ? '/archive/' : '#'}">${item.title}</a>` : item.title}</span><span class="separator"></span><span class="meta">${formatInlineMeta(item, metadataLevel)}</span>
                </li>`).join('')}
            </ul>`;
    }

    // Generate tab-aligned format HTML
    function generateTabAlignedList(classes) {
        return `<ul class="work-list ${classes}">
            ${workItems.map(item => {
                return `
                <li class="work-item">
                    <span class="project-name">${item.hasLink ? `<a href="${item.title === 'Archive Grid' ? '/archive/' : '#'}">${item.title}</a>` : item.title}</span>
                    <span class="meta-col">${item.year}</span>
                    <span class="meta-col">${item.location}</span>
                    <span class="meta-col">${item.collaborator}</span>
                    <span class="meta-col">${item.type}</span>
                </li>`;
            }).join('')}
            </ul>`;
    }

    // Generate grid format HTML
    function generateGridList(classes) {
        return `<ul class="work-list ${classes}">
            ${workItems.map(item => `
                <li class="work-item">
                    <div class="year">${item.year}</div>
                    <div class="project-info">
                        <div class="project-name">${item.hasLink ? `<a href="${item.title === 'Archive Grid' ? '/archive/' : '#'}">${item.title}</a>` : item.title}</div>
                        <div class="collaborator">${formatGridCollaborator(item)}</div>
                    </div>
                    <div class="type">${item.type}</div>
                </li>`).join('')}
            </ul>`;
    }

    // Build the page content
    const content = `
        <!-- SECTION 1: FONT FAMILY -->
        <div class="section-title">1. FONT FAMILY</div>
        <div class="mockup-grid">
            <div class="mockup">
                <div class="mockup-title">INTER</div>
                <div class="mockup-subtitle">Sans-serif · Modern · Clean</div>
                ${generateInlineList('font-inter spacing-medium hover-underline sep-comma')}
            </div>
            <div class="mockup">
                <div class="mockup-title">CORMORANT</div>
                <div class="mockup-subtitle">Serif · Elegant · Editorial</div>
                ${generateInlineList('font-cormorant spacing-medium hover-underline sep-comma')}
            </div>
            <div class="mockup">
                <div class="mockup-title">IBM PLEX MONO</div>
                <div class="mockup-subtitle">Monospace · Technical · Distinctive</div>
                ${generateInlineList('font-mono spacing-medium hover-underline sep-comma')}
            </div>
        </div>

        <!-- SECTION 2: VERTICAL SPACING -->
        <div class="section-title">2. VERTICAL SPACING</div>
        <div class="mockup-grid">
            <div class="mockup">
                <div class="mockup-title">TIGHT (14px)</div>
                <div class="mockup-subtitle">Compact · Dense · More entries visible</div>
                ${generateInlineList('font-inter spacing-tight hover-underline sep-comma')}
            </div>
            <div class="mockup">
                <div class="mockup-title">MEDIUM (20px)</div>
                <div class="mockup-subtitle">Balanced · Readable · Standard</div>
                ${generateInlineList('font-inter spacing-medium hover-underline sep-comma')}
            </div>
            <div class="mockup">
                <div class="mockup-title">LOOSE (28px)</div>
                <div class="mockup-subtitle">Generous · Luxurious · Spacious</div>
                ${generateInlineList('font-inter spacing-loose hover-underline sep-comma')}
            </div>
        </div>

        <!-- SECTION 3: HOVER EFFECT -->
        <div class="section-title">3. HOVER EFFECT TYPE</div>
        <div class="mockup-grid">
            <div class="mockup">
                <div class="mockup-title">UNDERLINE</div>
                <div class="mockup-subtitle">Border appears on hover · Classic · Subtle</div>
                ${generateInlineList('font-inter spacing-medium hover-underline sep-comma')}
            </div>
            <div class="mockup">
                <div class="mockup-title">COLOR SHIFT</div>
                <div class="mockup-subtitle">Text lightens on hover · Smooth · Minimal</div>
                ${generateInlineList('font-inter spacing-medium hover-color sep-comma')}
            </div>
            <div class="mockup">
                <div class="mockup-title">BRACKETS</div>
                <div class="mockup-subtitle">[ ] appear on hover · Technical · Playful</div>
                ${generateInlineList('font-inter spacing-medium hover-brackets sep-comma')}
            </div>
        </div>

        <!-- SECTION 4: SEPARATOR TYPE -->
        <div class="section-title">4. SEPARATOR TYPE</div>
        <div class="mockup-grid">
            <div class="mockup">
                <div class="mockup-title">COMMA</div>
                <div class="mockup-subtitle">Natural · Flowing · Editorial</div>
                ${generateInlineList('font-inter spacing-medium hover-underline sep-comma')}
            </div>
            <div class="mockup">
                <div class="mockup-title">SLASH</div>
                <div class="mockup-subtitle">Modern · Structured · Digital</div>
                ${generateInlineList('font-inter spacing-medium hover-underline sep-slash')}
            </div>
            <div class="mockup">
                <div class="mockup-title">EM DASH</div>
                <div class="mockup-subtitle">Elegant — Authoritative — Strong pause</div>
                ${generateInlineList('font-inter spacing-medium hover-underline sep-dash')}
            </div>
            <div class="mockup">
                <div class="mockup-title">TAB-ALIGNED</div>
                <div class="mockup-subtitle">Columnar · Clean · Code-inspired</div>
                ${generateTabAlignedList('font-inter spacing-medium hover-underline sep-tab')}
            </div>
        </div>

        <!-- SECTION 5: METADATA AMOUNT -->
        <div class="section-title">5. METADATA AMOUNT</div>
        <div class="mockup-grid">
            <div class="mockup">
                <div class="mockup-title">YEAR ONLY</div>
                <div class="mockup-subtitle">Minimal · Clean · Maximum focus on work</div>
                ${generateInlineList('font-inter spacing-medium hover-underline sep-comma', 'year-only')}
            </div>
            <div class="mockup">
                <div class="mockup-title">YEAR + LOCATION/COLLABORATOR</div>
                <div class="mockup-subtitle">Moderate · Informative · Contextual</div>
                ${generateInlineList('font-inter spacing-medium hover-underline sep-comma', 'year-collab')}
            </div>
            <div class="mockup">
                <div class="mockup-title">FULL METADATA</div>
                <div class="mockup-subtitle">Complete · Detailed · Comprehensive</div>
                ${generateInlineList('font-inter spacing-medium hover-underline sep-comma', 'full')}
            </div>
        </div>

        <!-- SECTION 6: FORMAT TYPE -->
        <div class="section-title">6. FORMAT TYPE</div>
        <div class="mockup-grid">
            <div class="mockup">
                <div class="mockup-title">INLINE</div>
                <div class="mockup-subtitle">Single line · Flowing · Natural reading</div>
                ${generateInlineList('font-inter spacing-medium hover-underline sep-comma')}
            </div>
            <div class="mockup">
                <div class="mockup-title">GRID</div>
                <div class="mockup-subtitle">Column-based · Structured · Scannable</div>
                ${generateGridList('font-inter format-grid')}
            </div>
        </div>
    `;

    document.getElementById('content').innerHTML = content;
</script>
{% endblock %}
